{
  "name": "govml-sites",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-sites.mjs",
    "start": "npm --prefix frontend run preview -- --host 0.0.0.0 --port ${PORT:-4173}"
  }
}
